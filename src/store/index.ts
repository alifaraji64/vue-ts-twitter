import { createStore } from 'vuex'

export default createStore({
  state: {
    base_url:'http://localhost:3000/' as string,
    count:0,
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
